name: Release

on:
  push:
    tags:
      - "v*"

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: write

jobs:
  create-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
      tag: ${{ steps.meta.outputs.tag }}
    steps:
      - name: Resolve version + tag
        id: meta
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          tag="${REF_NAME}"
          version="${tag#v}"
          echo "tag=${tag}"         >> "$GITHUB_OUTPUT"
          echo "version=${version}" >> "$GITHUB_OUTPUT"

      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          TAG: ${{ steps.meta.outputs.tag }}
          VERSION: ${{ steps.meta.outputs.version }}
        run: |
          if gh release view "${TAG}" --repo "${REPO}" >/dev/null 2>&1; then
            echo "Release ${TAG} already exists; skipping create."
            exit 0
          fi

          gh release create "${TAG}" \
            --repo "${REPO}" \
            --title "Release ${TAG}" \
            --notes "Release ${VERSION}

          ## Installation

          ### Homebrew
          \`\`\`bash
          brew tap ${REPO} https://github.com/${REPO}
          brew install octui
          \`\`\`

          ### Cargo
          \`\`\`bash
          cargo install octui
          \`\`\`

          ### macOS (Apple Silicon)
          \`\`\`bash
          curl -L \"https://github.com/${REPO}/releases/download/${TAG}/octui-${VERSION}-aarch64-apple-darwin.tar.gz\" | tar xz
          \`\`\`

          ### macOS (Intel)
          \`\`\`bash
          curl -L \"https://github.com/${REPO}/releases/download/${TAG}/octui-${VERSION}-x86_64-apple-darwin.tar.gz\" | tar xz
          \`\`\`

          ### Linux (x86_64)
          \`\`\`bash
          curl -L \"https://github.com/${REPO}/releases/download/${TAG}/octui-${VERSION}-x86_64-unknown-linux-gnu.tar.gz\" | tar xz
          \`\`\`"

  build-release:
    name: Build ${{ matrix.target }}
    needs: create-release
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
          - os: macos-15-intel
            target: x86_64-apple-darwin
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          target: ${{ matrix.target }}

      - name: Build release binary
        env:
          TARGET: ${{ matrix.target }}
        run: cargo build --release --target "${TARGET}"

      - name: Smoke test --version
        env:
          TARGET: ${{ matrix.target }}
          VERSION: ${{ needs.create-release.outputs.version }}
        run: |
          out=$("./target/${TARGET}/release/octui" --version)
          echo "${out}"
          echo "${out}" | grep -F "${VERSION}"

      - name: Create archive
        env:
          TARGET: ${{ matrix.target }}
          VERSION: ${{ needs.create-release.outputs.version }}
        run: |
          archive="octui-${VERSION}-${TARGET}.tar.gz"
          tar -C "target/${TARGET}/release" -czf "${archive}" octui
          ls -la "${archive}"
          echo "ARCHIVE=${archive}" >> "$GITHUB_ENV"

      - name: Upload release asset
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          TAG: ${{ needs.create-release.outputs.tag }}
        run: gh release upload "${TAG}" --repo "${REPO}" "${ARCHIVE}"

  update-homebrew-formula:
    name: Update Homebrew Formula
    needs: [create-release, build-release]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}

      - name: Compute SHA256 hashes
        id: sha
        env:
          REPO: ${{ github.repository }}
          TAG: ${{ needs.create-release.outputs.tag }}
          VERSION: ${{ needs.create-release.outputs.version }}
        run: |
          base="https://github.com/${REPO}/releases/download/${TAG}"

          aarch64_mac=$(curl -fsSL "${base}/octui-${VERSION}-aarch64-apple-darwin.tar.gz" | sha256sum | cut -d' ' -f1)
          x86_mac=$(curl     -fsSL "${base}/octui-${VERSION}-x86_64-apple-darwin.tar.gz"  | sha256sum | cut -d' ' -f1)
          x86_linux=$(curl   -fsSL "${base}/octui-${VERSION}-x86_64-unknown-linux-gnu.tar.gz" | sha256sum | cut -d' ' -f1)

          echo "aarch64_mac=${aarch64_mac}" >> "$GITHUB_OUTPUT"
          echo "x86_mac=${x86_mac}"         >> "$GITHUB_OUTPUT"
          echo "x86_linux=${x86_linux}"     >> "$GITHUB_OUTPUT"

      - name: Regenerate Formula/octui.rb
        env:
          REPO: ${{ github.repository }}
          TAG: ${{ needs.create-release.outputs.tag }}
          VERSION: ${{ needs.create-release.outputs.version }}
          AARCH64_MAC_SHA: ${{ steps.sha.outputs.aarch64_mac }}
          X86_MAC_SHA: ${{ steps.sha.outputs.x86_mac }}
          X86_LINUX_SHA: ${{ steps.sha.outputs.x86_linux }}
        run: |
          mkdir -p Formula
          cat > Formula/octui.rb <<EOF
          class Octui < Formula
            desc "GitHub contribution graph in your terminal"
            homepage "https://github.com/${REPO}"
            version "${VERSION}"
            license "MIT"

            on_macos do
              if Hardware::CPU.arm?
                url "https://github.com/${REPO}/releases/download/${TAG}/octui-${VERSION}-aarch64-apple-darwin.tar.gz"
                sha256 "${AARCH64_MAC_SHA}"
              else
                url "https://github.com/${REPO}/releases/download/${TAG}/octui-${VERSION}-x86_64-apple-darwin.tar.gz"
                sha256 "${X86_MAC_SHA}"
              end
            end

            on_linux do
              url "https://github.com/${REPO}/releases/download/${TAG}/octui-${VERSION}-x86_64-unknown-linux-gnu.tar.gz"
              sha256 "${X86_LINUX_SHA}"
            end

            def install
              bin.install "octui"
            end

            test do
              assert_match version.to_s, shell_output("#{bin}/octui --version")
            end
          end
          EOF

          echo "Generated formula:"
          cat Formula/octui.rb

      - name: Commit and push
        env:
          VERSION: ${{ needs.create-release.outputs.version }}
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add Formula/octui.rb
          if git diff --staged --quiet; then
            echo "No formula changes."
            exit 0
          fi
          git commit -m "chore: update homebrew formula to ${VERSION}"
          git push origin HEAD:main

  publish-crates:
    name: Publish to crates.io
    needs: [create-release, build-release]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable

      - name: Publish
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CRATES_IO_TOKEN }}
        run: cargo publish
